# 🔗 نوتة التركيب والتشغيل وربط الأجهزة — HR Pro System

وتشمل أنكل حاجة: **التركيب (مرة واحدة)**، ثم **التشغيل**، ثم **ربط الأجهزة** ببعضها في حالتين:
- **الحالة 1:** الأجهزة **جنب بعض** (نفس المكتب/المنزل) → شبكة الراوتر المحلية (LAN).
- **الحالة 2:** الأجهزة **بعيدة عن بعض** (فروع/مناطق مختلفة) → الجهاز الرئيسي نواة مركزية والفروع تصل إليه.

---

## 🧭 الفكرة الأساسية (مهم جداً)

```
            ┌─────────────────────────────────────────┐
            │   الجهاز الرئيسي (السيرفر / النواة)     │
            │   يعمل عليه النظام وقاعدة البيانات        │
            └───────────────┬─────────────────────────┘
                            │
        ┌───────────────────┼───────────────────────┐
        │                   │                       │
   أجهزة قريبة          أجهزة بعيدة            أجهزة البصمة
   (نفس الشبكة)        (فروع/مناطق أخرى)       (ترسل الحضور)
   http://IP:8080      https://رابط-public       عبر API آمن
```

- **قاعدة البيانات SQLite** على الجهاز الرئيسي فقط (ملف `hr_system.db`).
- كل الأجهزة الأخرى تستعمل **المتصفح فقط** — لا تثبيت ولا نسخ قاعدة بيانات لكل جهاز.
- هذا التصميم الصحيح: بيانات واحدة محدّثة للجميع + نسخة احتياطية واحدة (تلقائية يومياً 02:00).

---

# ⚙️ 1) التركيب (مرة واحدة — على الجهاز الرئيسي)

**المتطلبات:** ويندوز عليه **Python 3.11** (مثبّت مع الخيار "Add python.exe to PATH").

1. فكّ ضغط مجلد النظام `بصمة` إلى مكان ثابت (مثلاً `C:\بصمة`).
2. انقر مزدوجاً: **`install.bat`**
   - ينشئ بيئة افتراضية `.venv` ويثبّت الاعتماديات تلقائياً:
     `Flask` و `Flask-SQLAlchemy` و `reportlab` و `arabic-reshaper` و `python-bidi` و `openpyxl`.
   - يجهّز قاعدة البيانات والحسابات الافتراضية.
3. عند انتهائه سترى «`[√] تم التثبيت بنجاح`».

> إن لم يوجد `install.bat` أو كلّك الـ venv، بديل يدوي:
> ```
> python -m venv .venv
> .venv\Scripts\activate.bat
> pip install -r requirements.txt
> python init_db.py
> ```

---

# ▶️ 2) التشغيل

| السكربت | الاستخدام |
|---|---|
| `start_silent.bat` | عادي بصمت (pythonw) — للجهاز الرئيسي محلياً. |
| `start_console.bat` | **وضع المطور**: يعرض السجل مباشرة في نافذة (مفيد لحل المشاكل). |
| `start_network.bat` | الجهاز الرئيسي **على الشبكة**: يشغّل + يعرض رابط اللان + يضيف قاعدة جدار الحماية للمنفذ 8080. |
| `start_demo.bat` | وضع تجريبي معزول على `http://127.0.0.1:8080` (حساب `demo / demo1234`). |
| `stop.vbs` | إيقاف الخادم. |

- المنفذ الافتراضي **8080**؛ ولتغييره: `set PORT=9090` قبل أي سكربت تشغيل.
- سجلات التشغيل تُكتب في `logs\server.log` وملف `server.pid` يحمل رقم العملية.
- مهام خلفية تلقائية: **نسخ احتياطي 02:00**، **حساب الغياب 23:30**، **إعادة ضبط بيانات الديمو 03:00**.
- الدخول التلقائي الافتراضي: من **الجهاز الرئيسي فقط** (`127.0.0.1`)، وبقية الأجهزة تتطلب تسجيل دخول بحساب فعلي.

> متغير `AUTO_LOGIN=lan` يجعل كل شبكة الراوتر تدخل تلقائياً، و`AUTO_LOGIN=all` يجعل كل شيء يدخل تلقائياً — **استخدمها فقط في بيئة موثوقة**.

---

# ✅ 3) الحالة 1: الأجهزة جنب بعض (شبكة الراوتر المحلية)

Location: نفس مكتب أو نفس شبكة WiFi/كابل.

### على الجهاز الرئيسي
1. انقر مزدوجاً: **`start_network.bat`**
   - يشغّل النظام صامتاً.
   - يعرض عنوان الجهاز على الشبكة (مثل `http://192.168.1.10:8080`).
   - يضيف قاعدة جدار الحماية للمنفذ 8080 (وافق على صلاحية مدير إن طُلب).
2. احتفظ بالجهاز شغالاً — هو الخادم.

### على أي جهاز آخر (موظف/فرع قريب)
1. انقر مزدوجاً **`connect_client.bat`**.
2. اكتب عنوان الجهاز الرئيسي (الرقم الذي ظهر في الخطوة 1) → يُفتح النظام.
3. سجّل الدخول بحسابك.

> بدون سكربت: فقط افتح المتصفح على `http://192.168.1.10:8080`.

### لو فشل الفتح من الأجهزة الأخرى
- تأكد أن الجهازين على **نفس الشبكة** (لا «عزل العملاء» Client Isolation في إعدادات الراوتر).
- نفّذ على الجهاز الرئيسي كمسؤول إن لزم:
  ```
  netsh advfirewall firewall add rule name="HR Pro 8080" dir=in action=allow protocol=TCP localport=8080
  ```
- جرّب من الجهاز الآخر: `ping 192.168.1.10` (إن لم يستجب فالمشكلة شبكة لا برنامج).
- اكتب `http://` صراحةً (المتصفح يُجبر على https في بعض الحالات).

---

# 🌍 4) الحالة 2: الفروع بعيدة عن بعض (مناطق مختلفة)

الجهاز الرئيسي سيظل النواة، لكن يلزمه **عنوان يصل إليه الفروع من الإنترنت**:

## الطريق أ — الأسرع (نفق Cloudflare المؤقت للتجربة)
1. نزّل `cloudflared.exe` من:
   `https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/`
   وضعه بجانب ملفات النظام.
2. شغّل **`عرض_عام_تشغيل.bat`** (أو `نفق_للخارج.bat`):
   - يشغّل نسخة **DEMO معزولة** (بيانات تجريبية فقط) ويفتح نفقاً عاماً.
   - يعطيك رابط مثل: `https://xxxx.trycloudflare.com` — حساب `demo / demo1234`.
3. أرسل الرابط للمعاينة، أو استخدمه كعنوان يرسل إليه جهاز بصمة تجريبي.

**Sec: مهم —** النفق العام **لا يربط أبداً** بسيرفر 8080 الحقيقي (فيه بياناتك الفعلية). سكربت `عرض_عام_تشغيل.bat` يفتح نسخة ديمو معزولة ثم ينشئ النفق إليها فقط.
**انتبه:** الرابط يعمل **ما دام الجهاز والنافذة شغالين**، ويتغير في كل تشغيل.

## الطريق ب — الأفضل والدائم (سحابة مجانية)
- انشر نسخة **DEMO** على **PythonAnywhere** (مجاني، بلا بطاقة، شغال 24/7 حتى لو أُطفئ جهازك) — شرح كامل في **`دليل_الاستضافة.md`**.
- هنا «الجهاز الرئيسي» هو السحابة نفسها، والفروع كلها تتعامل معها.
- لأي بيانات حقيقية دائمة في الشركة، السحابة المجانية للعرض التجريبي فقط؛ البيانات الفعلية تبقى على جهاز المكتب مع VPN (الطريق ج) أو عنوان عام ثابت.

## الطريق ج — VPN للمكتب (الموظفون من بيوتهم يصلون لشبكتك الداخلية)
- ثبّت **ZeroTier** (أسهل) على الجهاز الرئيسي وعلى أجهزة الفروع.
- كل الأجهزة تصبح في شبكة واحدة افتراضية، والفرع يفتح على `http://<IP-شبكة-ZeroTier>:8080` مباشرة كأنه في المكتب.

> **القاعدة الذهبية:** بغضّ النظر عن الطريق — **البيانات على الجهاز الرئيسي فقط**، والباقي «عميل» عبر المتصفح، و**أجهزة البصمة** ترسل الحضور إلى عنوان السيرفر عبر API (الأقسام التالية).

---

# 🖐️ 5) ربط أجهزة البصمة

للنظام طريقتان لتلقي الحضور، حسب نوع الجهاز ومكانه:

### (أ) السحب المباشر — للأجهزة القريبة (نفس شبكة اللان)
- من اللوحة: **أجهزة البصمة ← إضافة جهاز**، وسجّل:
  - **الاسم**، **نوع الجهاز**:
    - `zkteco` — أجهزة ZKTeco (منفذ 4370 عبر pyzk).
    - `http` — أجهزة صينية بفوائد HTTP (الأكثر شيوعاً).
    - `tcp` — أجهزة TCP قديمة (استعلام سجل فقط).
    - `simulator` — محاكاة تجريبية بلا جهاز فعلي.
  - **عنوان IP للجهاز** مثل `192.168.1.50`.
- ثم اضغط **«مزامنة»** بجانب الجهاز → يُسحب كل تسجيلات اليوم ويُحتسب الدخول/الانصراف والتأخير تلقائياً حسب مواعيد العمل في الإعدادات.
- شرط النجاح: الجهاز والكمبيوتر على نفس الشبكة، والـ IP صحيح (`ping` يتأكد).

### (ب) الدفع عبر API — للأجهزة البعيدة (فروع/أونلاين) 🇺🇾
النظام يفتح واجهة API آمنة:

```
POST  /api/fingerprint/punch
Header:  X-API-Key: HR-XXXX...
Body:
{
  "fingerprint_id": "1001",
  "timestamp": "2024-01-15 18:30:00"
}
```

- **المفتاح** من صفحة **الإعدادات** (زر «توليد مفتاح جديد») أو تعرضه صفحة أجهزة البصمة.
- الجهاز/الموقع البعيد يرسل كل بصمة إلى عنوان السيرفر:
  - سيرفر لان: `http://192.168.1.10:8080/api/fingerprint/punch`
  - سيرفر إنترنت: `https://رابط-public/api/fingerprint/punch`
- **الرقم `fingerprint_id`** يجب أن يساوي رقم الموظف في النظام (حقل «رقم البصمة» ببطاقة الموظف).
- حماية: مفتاح API + حد 30 طلب/دقيقة لكل عنوان.

### (ج) بدائل يدوية
- تسجيل نقرة يدوية من صفحة **الحضور**.
- استيراد سجل حضور من ملف CSV دفعة واحدة (صيغ `zkteco` / `simplified`).
- اختبار جهاز بدون جهاز: جهاز `simulator` + صفحة **محاكاة البصمة** (تكتب سطراً في `logs\fingerprint_punches.txt` ثم تُزامن).

---

# 👥 6) حسابات المستخدمين والأدوار (v2.3)

## 🔑 حسابات الدخول الافتراضية بعد التثبيت (النسخة الفعلية)

عند أول تشغيل للنظام كامل (غير تجريبي) تُنشأ تلقائياً هاتين الحسابين:

| المستخدم | كلمة المرور | الصلاحية |
|---|---|---|
| `admin` | `admin123` | مدير النظام (كل الصلاحيات) |
| `hr` | `hr123` | مدير الموارد البشرية |

> **مهم:** غيّر كلمة المرور فور أول دخول من «الإعدادات» أو «المستخدمون». أما النسخة التجريبية (demo) فحسابها الوحيد `demo / demo1234` ولا تُنشأ فيها أي حسابات إدارية.

من **مستخدمو النظام** أنشئ حساباً لكل مستخدم/فرع، والأدوار:

| الدور | الصلاحيات |
|---|---|
| `admin` | كل شيء + الإعدادات + النسخ الاحتياطي + توليد مفتاح API. |
| `hr` | موارد بشرية: موظفون/حضور/إجازات/رواتب/تقارير/سجل النشاط. |
| `account` | محاسب/شؤون مالية: عرض كل شيء والتقارير المالية، **بلا** إضافة/تعديل موظف أو حضور أو إجازات أو أجهزة بصمة أو إعدادات. |
| `viewer` | مشاهدة فقط (كل التقارير بلا أي تعديل). |

- **سجل النشاط**: من القائمة الجانبية (admin/hr/account) — يرصد كل الإجراءات (مَن/متى/ماذا/من أي IP).
- **التقرير الشهري الموحّد**: تقارير ← «تقرير شهري موحّد PDF» (ملخص الشهر + كشف رواتب + حضور + إجازات/مكافآت/سلف + توقيعات).
- الجهاز الرئيسي يدخل تلقائياً، أما **الأجهزة الأخرى يُطلب منها تسجيل الدخول**. بعد أي جلسة اضغط **تسجيل الخروج**.

---

# 🗂️ سكربتات جاهزة (بمجلد النظام)

| السكربت | مكان التشغيل | وظيفته |
|---|---|---|
| `install.bat` | جهاز أي مكتب (مرة واحدة) | بيئة + اعتماديات + قاعدة بيانات. |
| `start_network.bat` | الجهاز الرئيسي | تشغيل + عرض رابط الشبكة + قاعدة جدار الحماية. |
| `connect_client.bat` | أي جهاز فرع | إدخال عنوان السيرفر وفتح النظام. |
| `عرض_عام_تشغيل.bat` / `نفق_للخارج.bat` | الجهاز الرئيسي | رابط عام آمن (DEMO معزول) للمعاينة أو الأجهزة البعيدة. |
| `start_silent.bat` / `start.vbs` | الجهاز الرئيسي | تشغيل عادي بصمت. |
| `start_console.bat` | المطور | تشغيل مع عرض السجل. |
| `start_demo.bat` | تجريبي | نسخة DEMO (`demo / demo1234`). |
| `stop.vbs` | الجهاز الرئيسي | إيقاف الخادم. |
| `reset_db.bat` | المطور | إعادة تهيئة قاعدة البيانات. |

---

# 🛠️ حل المشاكل الشائعة

**» الآخرون لا يفتحون الصفحة** → راجع جدار الحماية + نفس الشبكة + `ping`.
**» المتصفح يفتح https** → اكتب `http://` صراحة.
**» فرع بعيد لا يصل للرابط المؤقت** → النفق مؤقت؛ استخدم الطريق (ب) سحابة دائمة أو (ج) VPN.
**» جهاز البصمة لا يُسحب** → تأكد أن الـ IP صحيح وأن الجهاز على نفس الشبكة وعددي `ping` أولاً.
**» البصمة البعيدة لا تصل** → عنوان السيرفر في الجهاز = عنوان النفق/السحابة + مفتاح API مطابق من الإعدادات.
**» مزامنة ترجع «فشل الاتصال»** → نوع الجهاز غير مطابق؟ بعض أجهزة zkteco تحتاج منفذ 4370 مفتوحاً في جدار حماية الجهاز نفسه.
**» نسخة احتياطية** → تلقائية يومياً 02:00 في مجلد `backups`، ويمكن أخذها يدوياً من **الإعدادات ← نسخ احتياطي**.