# 🔗 نوتة ربط الأجهزة على الشبكة — HR Pro System

دليل عملي يشرح كيفية تشغيل النظام على **جهاز رئيسي واحد** (سيرفر) ورفع باقي **أجهزة المكتب** عليه عبر الشبكة الداخلية، وكيفية ربط **أجهزة البصمة**، وإتاحته من خارج المكتب عند الحاجة.

---

## 🖥️ السيناريو الأبسط (شبكة المكتب الداخلية)

```
┌─────────────┐        ┌─────────────┐
│  جهاز السيرفر  │  الشبكة   │  أجهزة الموظفين  │
│  (مثبّت عليه   │ ────────► │  (متصفح فقط)    │
│   النظام)    │   WiFi/  │  http://IP:8080  │
└─────────────┘   كابل    └─────────────┘
```

1. **الجهاز الرئيسي:** ثبّت المتطلبات وشغّل النظام:
   - ملف exe: شغّل `HR System.exe` فقط.
   - أو بالكود: `start_silent.bat` ثم يتولى التشغيل (بورت 8080 افتراضياً).
2. **اعرف عنوان الجهاز الرئيسي على شبكتك** (أي أمر مما يلي من موجه الأوامر):

   ```bat
   ipconfig
   ```
   ابحث عن `IPv4 Address` (مثل `192.168.1.10`) أسفل محول الشبكة النشط.

   **بدائل سريعة:**
   ```bat
   hostname          :: اسم الجهاز (ينفع يُكتب في المتصفح بدل الـ IP)
   ```
3. **من أي جهاز آخر على نفس الشبكة افتح المتصفح:** اكتب
   ```
   http://192.168.1.10:8080
   ```
   وسيعمل معك إن كانت الشبكة LAN/WiFi واحدة.

---

## 🔥 السماح لجدار الحماية (Firewall) — خطوة ضرورية

إذا فتحت الصفحة من الأجهزة الأخرى **ولم تفتح**، فعلّق منطق Windows Defender:

```bat
:: تشغيل موجه الأوامر كمدير (Administrator) ثم:
netsh advfirewall firewall add rule name="HR Pro 8080" dir=in action=allow protocol=TCP localport=8080
```

> شغّل الأمر مرة واحدة فقط على **جهاز السيرفر**، ويمكن حذف القاعدة لاحقاً:
> `netsh advfirewall firewall delete rule name="HR Pro 8080"`

**فشل الاتصال رغم ذلك؟** تأكد أن:
- الجهازان على **نفس الشبكة** (أو نفس شبكة WiFi للضيوف لا تفصل الأجهزة).
- موجه الشبكة/الراوتر لا يفعّل «عزل العملاء» / Client Isolation.
- برنامج مكافح فيروسات أطراف ثالثة لا يحجب المنفذ 8080.

---

## 📝 حسابات المستخدمين (كل جهاز بحسابه)

من لوحة الإدارة: **مستخدمو النظام ← إضافة مستخدم** وأنشئ حساباً لكل موظف أو لقسم:
- **admin** — مدير النظام (كل الصلاحيات + النسخ الاحتياطي + الإعدادات).
- **hr** — مدير الموارد البشرية (بيانات/إجازات/رواتب/تقارير/شهادات).
- **مستخدم عادي** — لحضور وانصراف ورصيد إجازاته فقط.

> أثناء وجودك أنت متصل، افتح `http://IP:8080/users` لإدارة الحسابات.
> في وضع **DEMO_MODE** (الاستضافة العامة) لا يظهر حساب admin إطلاقاً — حساب `demo/demo1234` فقط.

---

## 🖐️ ربط أجهزة البصمة بالشبكة

النظام يفتح **API آمن**: `http://IP:8080/api/attendance/push`

- من جهاز البصمة أو برنامج المناولة (الاتصال لتسجيل الحضور) أرسل طلباً على الرابط أعلاه بالمفتاح المسجّل في **الإعدادات ← أجهزة البصمة**.
- المفتاح مطلوب مع كل طلب (حماية من الدخول العشوائي).
- من صفحة **أجهزة البصمة** أضف جهاز الشبكة وسيتكلمان معاً داخل نطاق المكتب.
- الدخول الوارد يحتسب تلقائياً: تأخير/غياب/إجازة حسب مواعيد العمل في الإعدادات.

> بدون جهاز بصمة يمكن تسجيل الحضور يدوياً من صفحة الحضور، أو استيراده دفعة واحدة من ملف **CSV**.

---

## ✅ النص الجاهز للَّصق (تشغيل الخدمة على الجهاز الرئيسي)

```bat
:: مرة واحدة فقط عند أول تشغيل
install.bat

:: التشغيل الصامت + فتح المتصفح تلقائياً
start_silent.bat
```

ثم أرْسل لزملائك: `http://192.168.1.10:8080`

---

## 🌐 إتاحة النظام من خارج المكتب (إنترنت)

| الطريقة | المناسب | ملاحظات |
|---|---|---|
| **نشر نسخة DEMO على Render** (مجاني) | التجربة خارجياً / النشر العام | القراءة من `دليل_الاستضافة.md` — يبقى شغالاً حتى لو أقفل جهازك |
| **VPN للمكتب** (WireGuard/ZeroTier) | موظفون من منازلهم يصلون للنظام الداخلي | الشبكة شبكة واحدة افتراضية؛ الروابط الداخلية نفسها تعمل |
| **نفق مؤقت** (`cloudflared tunnel`) | معاينة سريعة من إنترنت | **مشروط بأن يظل جهازك شغالاً** — النفق يقع إذا انقطع الجهاز |

**القاعدة الذهبية:** لأي شيء «يستمر حتى لو أقفل الجهاز» استخدم نسخة **DEMO على Render/PythonAnywhere**، أما تشغيل «التعاون الداخلي» بمكتبك فسيرفر المكتب المحلي هو الأنسب (والأسرع بلا إنترنت).

---

## 🛠️ حل المشاكل الشائعة

**» الآخرون لا يفتحون الصفحة** → راجع خطوات جدار الحماية + نفس الشبكة + تجربة `ping 192.168.1.10` من جهازهم.

**» يستخدم المتصفح HTTPS إجبارياً** → اكتب `http://` صراحةً أمام الرابط (الرابط المحلي لا يحتاج HTTPS).

**» لا يظهر اسمي في النظام** → أنشئ حسابك من `مستخدمو النظام`، دوّن كلمة السر، وسجّل دخولك من جهازك.

**» جهاز البصمة لا يرسل** → تأكد أن عنوان الخادم المُدخل في الجهاز = `http://IP:8080` والمفتاح مطابق لإعدادات النظام.