# 🔗 نوتة ربط الأجهزة على الشبكة — HR Pro System

تشرح هذه النوتة **كيف يجلس النظام على جهاز رئيسي واحد وتشتغل عليه بقية الأجهزة**، في حالتين:
- **الحالة 1:** الأجهزة جنب بعض (نفس المكتب/المنزل) → شبكة الراوتر المحلية.
- **الحالة 2:** الأجهزة **بعيدة عن بعض** (فروع ومناطق مختلفة) → الجهاز الرئيسي يعمل كنواة مركزية ويصل إليه الفروع من الإنترنت.

---

## 🧭 الفكرة الأساسية (مهم جداً)

```
            ┌─────────────────────────────────────────┐
            │   الجهاز الرئيسي (السيرفر / النواة)     │
            │   يعمل عليه النظام وقاعدة البيانات        │
            └───────────────┬─────────────────────────┘
                            │
        ┌───────────────────┼───────────────────────┐
        │                   │                       │
   أجهزة قريبة          أجهزة بعيدة            أجهزة البصمة
   (نفس الشبكة)        (فروع/مناطق أخرى)       (ترسل الحضور)
   http://IP:8080      https://رابط-public     عبر API آمن
```

- **قاعدة البيانات SQLite** موجودة على الجهاز الرئيسي فقط (ملف `hr_system.db`).
- كل الأجهزة الأخرى تستعمل **المتصفح فقط** — لا تثبيت ولا نسخ بيانات لكل جهاز.
- هذا هو التصميم الصحيح: بيانات واحدة محدثة للجميع، واحتياطي واحد.

---

# ✅ الحالة 1: الأجهزة جنب بعض (شبكة الراوتر المحلية)

Location: نفس مكتب أو نفس شبكة WiFi/كابل.

### على الجهاز الرئيسي

1. انقر مزدوجاً: **`start_network.bat`**
   - يشغّل النظام صامتاً.
   - يعرض عنوان الجهاز على الشبكة (مثل `http://192.168.1.10:8080`).
   - يضيف تلقائياً **قاعدة جدار الحماية** للمنفذ 8080 (إن طُلب منه صلاحية مدير وافق).
2. احتفظ بالجهاز شغالاً — هو الخادم.

### على أي جهاز آخر (موظف/فرع قريب)

1. انقر مزدوجاً **`connect_client.bat`**
2. اكتب عنوان الجهاز الرئيسي (الرقم الذي ظهر في الخطوة 1) → يُفتح النظام مباشرة.
3. سجّل الدخول بحسابك.

> بدون سكربت: فقط افتح المتصفح واكتب `http://192.168.1.10:8080`.

### لو فشل الفتح من الأجهزة الأخرى

- تأكد أن الجهازين على **نفس الشبكة** (لا «عزل العملاء» Client Isolation في الراوتر).
- نفّذ على الجهاز الرئيسي كمسؤول:
  ```
  netsh advfirewall firewall add rule name="HR Pro 8080" dir=in action=allow protocol=TCP localport=8080
  ```
- جرّب من الجهاز الآخر: `ping 192.168.1.10` (إن لم يستجب فالمشكلة شبكة لا برنامج).
- اكتب `http://` صراحةً (المتصفح يُجبر على https أحياناً للمواقع فقط).

---

# 🌍 الحالة 2: الفروع بعيدة عن بعض (مناطق مختلفة)

الجهاز الرئيسي سيظل النواة، لكن يلزمه **عنوان يصل إليه الفروع من الإنترنت**. أمامك طريقان:

## الطريق أ — الأسرع (نفق Cloudflare المؤقت)

على الجهاز **الرئيسي**:
1. نزّل `cloudflared.exe` من:
   `https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/`
   وضعه بجانب ملفات النظام.
2. شغّل: **`نفق_للخارج.bat`**
   - يشغّل النظام (إن لم يكن يعمل).
   - ينشئ لك رابطاً عاماً مثل: `https://xxxx.trycloudflare.com`
3. أرسل الرابط إلى فروعك (أو ضعه كواجهة أجهزة البصمة).

**انتبه:** الرابط يعمل **ما دام الجهاز الرئيسي والنافذة شغالين**. الرابط يتغير في كل تشغيل.

## الطريق ب — الأفضل والدائم (سحابة مجانية بعيداً عن جهازك)

لو أردت أن **تغلق الجهاز الرئيسي** وتبقى الفروع تعمل:
- انشر نسخة **DEMO** على **Render** (مجاني): شرح كامل في **`دليل_الاستضافة.md`**.
- هنا الجهاز "الرئيسي" يصبح **السحابة نفسها**، والفروع كلها تتعامل معها، ونسخة الاستضافة معزولة ببيانات معاينة تُعاد يومياً بلا حسابات إدارية.

## الطريق ج — VPN للمكتب (موظفون من المنازل يصلون لشبكتك الداخلية)

- ثبّت **ZeroTier** أو **WireGuard** على الجهاز الرئيسي وعلى أجهزة الفروع.
- كل الأجهزة تصبح في شبكة واحدة افتراضية، ويفتح الفرع على نفس الرابط الداخلي (IP ثابت من الشبكة الافتراضية).

> **القاعدة الذهبية:** بغض النظر عن الطريق، **البيانات على الجهاز الرئيسي فقط**، والباقي «عميل» عبر المتصفح، و**جهاز البصمة الواحد** في كل فرع يرسل الحضور مباشرة إلى عنوان السيرفر عبر API.

---

# 🖐️ ربط أجهزة البصمة (أهم نقطة للفروع البعيدة)

النظام يفتح واجهة API آمنة:

```
http://IP-السيرفر:8080/api/attendance/push
```

- من صفحة **أجهزة البصمة** باللوحة: أضف كل جهاز بصمة من كل فرع (عنوانه = عنوان السيرفر + المفتاح المسجل).
- جهاز البصمة في فرع الإسكندرية يرسل بصمات الموظف إلى السيرفر الرئيسي (سواء على اللان أو عبر الإنترنت) بشكل مباشر.
- الدخول الوارد يُحتسب تلقائياً (تأخير/غياب/إجازة) حسب مواعيد العمل الموحدة في الإعدادات.

> بديل يدوي: تسجيل الحضور من صفحة الحضور، أو رفع ملف CSV دفعة واحدة.

---

# 🗂️ سكربتات جاهزة (بمجلد النظام)

| السكربت | مكان التشغيل | وظيفته |
|---|---|---|
| `start_network.bat` | الجهاز الرئيسي | تشغيل الخادم + عرض رابط الشبكة + قاعدة جدار الحماية |
| `connect_client.bat` | أي جهاز فرع | إدخال عنوان السيرفر وفتح النظام تلقائياً |
| `نفق_للخارج.bat` | الجهاز الرئيسي | رابط عام مؤقت للفروع البعيدة (عبر Cloudflare) |
| `start_silent.bat` / `start.vbs` | الجهاز الرئيسي | تشغيل عادي بصمت (بدون عرض الشبكة) |
| `stop.vbs` | الجهاز الرئيسي | إيقاف الخادم |
| `start_demo.bat` | للنشر التجريبي | نسخة DEMO (حساب demo/demo1234 فقط) |

---

# 👥 حسابات المستخدمين

من لوحة **مستخدمو النظام** أنشئ حساباً لكل مستخدم/فرع:
- **admin** — مدير النظام (كل الصلاحيات + الإعدادات + النسخ الاحتياطي).
- **hr** — مدير الموارد البشرية (موظفون/إجازات/رواتب/تقارير/شهادات).
- **مستخدم عادي** — حضوره/انصرافه ورصيد إجازاته فقط.

> الجهاز الرئيسي يدخل تلقائياً، أما **الأجهزة الأخرى تُطلب منها تسجيل الدخول** — وأنشئ لها حسابات فعلية. بعد كل جلسة اضغط **تسجيل الخروج**.

---

# 🛠️ حل المشاكل الشائعة

**» الآخرون لا يفتحون الصفحة** → راجع جدار الحماية + نفس الشبكة + `ping`.
**» المتصفح يفتح https** → اكتب `http://` صراحة.
**» فرع بعيد لا يصل للرابط المؤقت** → النفق مؤقت؛ استخدم طريقتَني (ب) سحابة دائمة أو (ج) VPN.
**» جهاز البصمة لا يرسل** → تأكد أن العنوان المُدخل بالجهاز = عنوان السيرفر (`http://…:8080`) والمفتاح مطابق، وأن النفق/الشبكة مفتوحان إليه.