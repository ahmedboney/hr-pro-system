# ☁️ دليل الاستضافة التجريبية — HR Pro System

شرح نَشْر النسخة التجريبية (وضع DEMO) على منصتين مجانيتين بدون أي حساب إداري في الملفات المرفوعة.

> 👉 **بدون بطاقة إطلاقاً؟** استخدم **PythonAnywhere** (القسم 2): مجاني بالكامل، **لا يطلب بطاقة بنكية**، ويبقى رابطك شغالاً **24/7 حتى إذا أُطفئ جهازك**. (Render قد يطلب بطاقة للتحقق حتى على خطته المجانية — ليس إلزامياً لاستخدامنا.)

## 1) النشر على Render (زر واحد — إن رغبت)

1. ارفع المشروع إلى GitHub (إرشادات أسفل الصفحة).
2. سجّل في https://render.com (بريدك أو GitHub).
3. من اللوحة: **New → Blueprint**.
4. الصق رابط المستودع (مثال: `https://github.com/ahmedboney/hr-pro-system`).
5. سيقرأ Render ملف `render.yaml` تلقائياً وينشئ خدمة ويب مجانية:
   - `DEMO_MODE=1` مفعّل (بلا حسابات admin).
   - `SESSION_COOKIE_SECURE=1` (HTTPS كامل).
   - `SECRET_KEY` مولّد تلقائياً.
6. اضغط **Apply** وانتظر 3-5 دقائق للبناء. ستظهر الخدمة على رابط مثل:
   `https://hr-pro-system.onrender.com`
7. أدخل من الرابط وحاذر: `demo / demo1234`.

> ملاحظة: خطة Render المجانية تُوقف الخدمة بعد خمول؛ أول زيارة تستغرق ~30 ثانية لتشغيلها من جديد.

## 2) النشر على PythonAnywhere — الموصى به بدون بطاقة (مجاني، يعمل 24/7)

### 2.1 أنشئ الحساب (لا يطلب بطاقة):
1. افتح **https://www.pythonanywhere.com** → **Start running Python online in less than a minute!** → ثم **Create a Beginner account** (مجاني).
2. أدخل بريدك + اسم مستخدم (سيصبح جزءاً من الرابط، مثل `ahmedbrown`).
3. اضغط **لينك التأكيد** في بريدك.

### 2.2 حمّل الكود:
طريقة (أ) — عبر رفع الملف مباشرة (أنصح بها، بدون GitHub):
- من Dashboard → **Files** تبويب → افتح مجلدك (http://www.pythonanywhere.com/user/<اسمك>/files/) → زر **Upload** أعلى النافذة.
- ارفع الملف **`HR_Pro_System_Source_v2.6.zip`** من جهازك (مجلد `releases`).
- افتح **Consoles → Bash** وأنفّذ:
  ```bash
  mkdir -p hr_system && cd hr_system && unzip -o ../HR_Pro_System_Source_v2.6.zip
  ```
  (إن لم تعمل `unzip`، استخدم بديلها: `python3 -m zipfile -e ../HR_Pro_System_Source_v2.6.zip .`)

طريقة (ب) — عبر GitHub (إن أردت):
```bash
git clone https://github.com/ahmedboney/hr-pro-system hr_system
```

### 2.3 أنشئ بيئة افتراضية وثبّت المتطلبات (في نفس الـ Bash):
```bash
cd ~/hr_system
python3 -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
```

### 2.4 أضف موقع الويب:
- **Web** تبويب → **Add a new web app** → اختر **Manual configuration** → ثم أخر إصدار **Python 3.x** المتاح.
- في حقل **Code**: احفظ `project_home = '/home/<اسمك>/hr_system'` واترك Virtualenv فارغاً (سنملؤه بعد).
- اربط البيئة الافتراضية: في إعداد الموقع انقر **Virtualenv** وأدخل `/home/<اسمك>/hr_system/.venv`.

### 2.5 عدّل ملف **WSGI configuration file** إلى:
```python
import os, sys
os.environ['DEMO_MODE'] = '1'
os.environ['SESSION_COOKIE_SECURE'] = '1'
project_home = '/home/<اسمك>/hr_system'
sys.path.insert(0, project_home)
if project_home not in sys.path:
    sys.path.insert(0, project_home)
from app import app as application
```

### 2.6 فعّل بـ **Reload**:
- الرابط النهائي يصبح: **`https://<اسمك>.pythonanywhere.com`**
- ادخل منه: `demo / demo1234`.
- قاعدة SQLite تُخزَّن داخل مجلد `hr_system/hr_system.db` وتبقى محفوظة على حسابك.
- **استمرارية البيانات:** البيانات التي تدخلها في وضع الديمو تبقى محفوظة — بمجرد أن تحفظ أي إعداد أو تضيف موظفاً يُعلَّم النظام كمُعد، ولن تمسح إعادة الضبط التلقائية اليومية أي شيء بعد ذلك.

> ملاحظات الخطة المجانية: لا بطاقة، يعمل باستمرار، حصة أقراص 512MB (كافية تماماً)، حد CPU يومي (يكفي لعرض تجريبي)، وينام أول 10 ثوانٍ لا شيء (يبقى مفتوحاً).

## 3) التشغيل محلياً على الشبكة (للمكاتب)

- شغّل `start_silent.bat` على جهاز واحد، وادخل من باقي الأجهزة:
  `http://<عنوان-IP-الجهاز>:8080`
- كل موظف يزد أحسابه من صفحة «المستخدمون» للمدير.
- شرح مفصل (عنوان IP + جدار الحماية + أجهزة البصمة + الإتاحة من الخارج) في ملف **`نوتة_ربط_الاجهزة.md`**.

## 4) تسلسل الرفع إلى GitHub (محضر النشر)

```bash
cd "C:\Users\Boney\Desktop\بصمة"
git init
git add -A
git commit -m "HR Pro System: النسخة العامة الأولى"
git branch -M main
git remote add origin https://github.com/ahmedboney/hr-pro-system.git
git push -u origin main
```

**قبل الرفع تأكد (موجود بالفعل عبر .gitignore):**
- لا `hr_system.db` ولا `secret.key` ولا `server.pid` ولا مجلدات `backups/logs/build/dist/releases`.
- المفاتيح الحساسة تُنشأ تلقائياً في أول تشغيل.
- الحسابات الافتراضية `admin/admin123` لاتُعرض في وضع DEMO إطلاقاً.

> ⚠️ بعد النشر على Render اعرض المنشور فيسبوك واضعاً رابط العرض التجريبي، أو أضفه في `منشور_فيسبوك.md`.